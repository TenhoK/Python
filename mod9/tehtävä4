
class KilpaAuto:
    def __init__(self, rekkari, huippu, nopeus, matka):
        self.rekkari = rekkari
        self.huippu = huippu
        self.nopeus = nopeus
        self.matka = matka

    def Kiihdytä(self, nopeus):
        self.nopeus = self.nopeus + nopeus

        if self.nopeus > self.huippu:
            self.nopeus = self.huippu

        if self.nopeus < 0:
            self.nopeus = 0

    def Kulje(self, matka):
        self.matka = self.matka + matka * self.nopeus
        #print("Auton kulkema matka:", self.matka)

import random

kilpaAutot = []

for i in range(1, 11):
    huippu = random.randint(100,200)
    rekkari = "ABC-" + str(i)
    nopeus = 0
    matka = 0
    uusiAuto = KilpaAuto(rekkari, huippu, nopeus, matka)
    kilpaAutot.append(uusiAuto)

maalissa = False

while maalissa == False:
    
    for auto in kilpaAutot:
        auto.Kulje(1)
        muutos = random.randint(-15,10)
        auto.Kiihdytä(muutos)

    for auto in kilpaAutot:
        if auto.matka >= 10000:
            maalissa = True

print("KILPAILU ON OHI !!!")

for auto in kilpaAutot:
    print("Rekkari:", auto.rekkari, "Hippunopeus:", auto.huippu, "Nopeus:", auto.nopeus, "Matka:", auto.matka)