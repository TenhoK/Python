name = input("Mikä nimesi on? ")

print("")
print("Hei", name,"!")
print("")

age = int(input("Kuinka vanha olet? "))
inventaario = []

def LisääEsine():
    print("")
    esine = input("Mitä tahdot lisätä reppuun? ")
    inventaario.append(esine)

def NäytäInventaario():
    if len(inventaario) == 0:
        print("Reppusi on tyhjä.")
    else:
        print("")
        print("Repussasi on:")
        for esine in inventaario:
            print(esine)

print("")
print("Olet", age, "vuotta vanha!")

if age < 12:
    print("Olet liian nuori pelaamaan tätä peliä :(")
    exit()
else:
    print("Tervetuloa pelaamaan", name,"!")

print("TENHON PELI PROJEKTI")
print("")
print("")
vastaus = input("(jatka) jatka peliä, (lisää) lisää inventoriin, (näytä) näytä inventori, (lopeta) lopeta peli ")

while True:
    
    if vastaus == "jatka":
        print("Jatketaan peliä!")

    if vastaus == "lisää":
        LisääEsine()

    if vastaus == "näytä":
        NäytäInventaario()

    if vastaus == "lopeta":
        print("Peli loppui ")
        break

    print("TENHON PELI PROJEKTI")
    print("")
    print("")
    vastaus = input("(jatka) jatka peliä, (lisää) lisää inventoriin, (näytä) näytä inventori, (lopeta) lopeta peli ")

